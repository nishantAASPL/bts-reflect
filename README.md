# bts-reflectai

A Vite + React application for BTS reflection and report generation.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Notes

- `node_modules/` and build artifacts are excluded via `.gitignore`.
- Update this README with usage, deployment, and contribution details as needed.
